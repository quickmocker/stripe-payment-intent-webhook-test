{
    "name": "quickmocker/stripe-payment-intent-webhook-test",
    "description": "Sample app and api to create Stripe payment intent and intercept Stripe notification using webhook service and QuickMocker's Local Forwarder",
    "require": {
        "stripe/stripe-php": "^7.66"
    },
    "autoload": {
        "psr-4": {
            "api\\": "api/"
        }
    }
}
